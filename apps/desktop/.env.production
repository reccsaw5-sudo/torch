# 生产构建(vite build / 打包客户端)时,桌面端连接的服务端地址。
# 开发环境不受影响,仍走默认 http://127.0.0.1:8080。
VITE_TORCH_SERVER=https://claw.torchai.ai

# 内置推理网关(new-api)域名根。写死进客户端:拉模型 + 推理全程直连,
# 不经过业务服务器。换域名需重新打包。
VITE_TORCH_INFERENCE_BASE=https://torchai.ai
