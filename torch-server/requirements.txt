# Torch backend (business server: login + brand + admin console).
# Install into the repo venv:  uv pip install -r torch-server/requirements.txt
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
psycopg[binary,pool]>=3.2,<4
pydantic>=2.7,<3
