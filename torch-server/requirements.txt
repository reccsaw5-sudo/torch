# Torch backend (business server + metering proxy + payments).
# Install into the repo venv:  uv pip install -r torch-server/requirements.txt
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
psycopg[binary,pool]>=3.2,<4
pydantic>=2.7,<3
# Payments
wechatpayv3>=1.3,<2
python-alipay-sdk>=3,<4
segno>=1.6,<2
